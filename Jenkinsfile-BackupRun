pipeline {
    agent {
        label 'docker_build'
    }

    environment {
        FULL_DOCKER_POSTGRES_IMAGE_NAME = 'thevirtualbrain/okd_postgresql_backups'
        FULL_DOCKER_FOLDER_IMAGE_NAME = 'thevirtualbrain/okd_rsync_backups'
    }

    stages {


        stage ('Build okd_postgresql_backups docker image') {
            steps {
                script {
                    VERSION_TAG = "1.2"
                    withDockerRegistry(credentialsId: 'tvb_on_docker_hub') {
                        def dockerImage = docker.build("${FULL_DOCKER_POSTGRES_IMAGE_NAME}:'${VERSION_TAG}'", "postgres-backup")
                        dockerImage.push()
                        dockerImage.push('latest')
                    }
                }
            }
        }

        stage ('Build okd_folder_backups docker image') {
            steps {
                script {
                    VERSION_TAG = "1.0"
                    withDockerRegistry(credentialsId: 'tvb_on_docker_hub') {
                        def dockerImage = docker.build("${FULL_DOCKER_FOLDER_IMAGE_NAME}:'${VERSION_TAG}'", "folder-backup")
                        dockerImage.push()
                        dockerImage.push('latest')
                    }
                }
            }
        }
    }
    post {
        changed {
            mail to: 'rares.horge@codemart.ro',
            subject: "Jenkins Pipeline ${currentBuild.fullDisplayName} changed status",
            body: """
                Result: ${currentBuild.result}
                Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]'
                Check console output at ${env.BUILD_URL}"""
        }
    }
}